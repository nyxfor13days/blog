---
import { NAV_ITEMS } from "../scripts/CONSTANTS";
---

<nav>
  <ul class="flex items-center gap-4 list-none">
    {
      NAV_ITEMS.map((item) => (
        <li>
          <a href={item.path}>{item.label}</a>
        </li>
      ))
    }
  </ul>
</nav>
